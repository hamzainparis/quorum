export const environment = {
  apiUrl: 'http://localhost:3000/api',
  socketUrl: 'http://localhost:3000',
  // Must match the GOOGLE_CLIENT_ID configured on quorum-api (see README).
  googleClientId: '',
};
